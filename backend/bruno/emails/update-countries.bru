meta {
  name: Update Target Countries
  type: http
  seq: 15
}

put {
  url: http://localhost:4000/api/emails/countries
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "countries": ["INDIA", "US"]
  }
}
