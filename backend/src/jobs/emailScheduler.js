// No cron scheduler - emails are sent manually by user clicking "Send" button
// This file is kept as placeholder for future scheduled tasks if needed

function initScheduler() {
  console.log('[Scheduler] No cron jobs configured - emails are sent manually.');
}

module.exports = { initScheduler };
